function toggleInfoPanel() {
    const panel = document.getElementById('info-panel');
    panel.classList.toggle('open');
}

function openNoteModal() {
    // Implémentation pour ouvrir le modal de création de note
}

// Code AJAX pour charger les notes selon la catégorie et les types d'utilisateurs
